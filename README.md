HSE SPb speech course homeworks repo. I'll probably change this file later but now I just recommend you to install
everything from `requirements.txt`.

That's done by running `pip install -r requirements.txt`.
